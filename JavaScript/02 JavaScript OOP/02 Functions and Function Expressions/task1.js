'use strict'

function solve(args) {
    let numbers = [];

    for (var i = 0; i < args.length; i += 1) {
        numbers[i] = +args[i];
    }




}


solve([1, 2, 3]);
solve([1, 2, 3]);
solve([]);
solve(['1', '2']);